import Vue from 'vue';

import Default from '@/layouts/Default';
import WithHeader from '@/layouts/WithHeader';

Vue.component('default-layout', Default);
Vue.component('with-header-layout', WithHeader);
